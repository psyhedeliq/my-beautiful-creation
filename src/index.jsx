import React from 'react';
import { render } from 'react-dom';
import MadCreation from './component/app';

const renderBlock = document.querySelector('#renderMyCreation');

render(<MadCreation />, renderBlock);
